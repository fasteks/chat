const initialState = {
  token: '',
  user: {
    name: 'fasteks',
    id: '28',
    isOnline: true
  }
}
/* eslint-disable default-param-last */
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
