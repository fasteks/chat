const initialState = {
  token: '',
  user: {
    name: 'John',
    id: '28'
  }
}
/* eslint-disable default-param-last */
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
